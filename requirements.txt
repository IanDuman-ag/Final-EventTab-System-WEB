# Django Framework
Django==6.0.3

# PostgreSQL Database Adapter
psycopg2-binary==2.9.12

# Python Environment Variables (for secure configuration)
python-decouple==3.8

# Pillow for Image Processing (if needed for user avatars, event images)
# Pillow==10.3.0  # Commented out due to build issues on Python 3.14

# Django REST Framework (if you plan to build APIs)
# djangorestframework==3.15.1  # Optional - install when needed

# CORS Headers (for API access from frontend)
# django-cors-headers==4.3.1  # Optional - install when needed

# Timezone support
pytz==2024.1
